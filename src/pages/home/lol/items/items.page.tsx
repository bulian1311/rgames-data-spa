import { LolLayout, Items } from '@components';
import { TProps } from './items.props';

export const ItemsPage = (props: TProps) => {
  return (
    <LolLayout>
      <Items />
    </LolLayout>
  );
};
