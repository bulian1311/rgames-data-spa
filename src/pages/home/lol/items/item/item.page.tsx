import { Headline, LolLayout } from '@components';
import { TProps } from './item.props';

export const ItemPage = (props: TProps) => {
  return (
    <LolLayout>
      <Headline>Item</Headline>
    </LolLayout>
  );
};
