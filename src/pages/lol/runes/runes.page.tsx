import { Headline, LolLayout } from '@components';
import { TProps } from './runes.props';

export const RunesPage = (props: TProps) => {
  return (
    <LolLayout>
      <Headline>Runes</Headline>
    </LolLayout>
  );
};
